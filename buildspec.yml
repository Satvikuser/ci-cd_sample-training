version: 0.2

phases:
  install:
    commands:
      - echo installing NGINX

  build:
    commands:
      - echo Build started on `date`

  post_build:
    commands:
      - echo Configuring NGINX

artifacts:
  files:
    - index.html

